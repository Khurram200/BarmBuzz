# 1) Ensure DSC module is installed
Install-PSResource -Name ActiveDirectoryDsc -Version 6.6.0 -Scope AllUsers -TrustRepository -Reinstall

# 2) Verify DSC resources exist
Get-DscResource -Module ActiveDirectoryDsc -Name ADDomain,ADOrganizationalUnit,ADComputer

# 3) Clear prereq flags so prereq phase can rerun if needed
Remove-Item "C:\BarmBuzz-main\Evidence\prereq_lcm_complete.flag" -ErrorAction SilentlyContinue
Remove-Item "C:\BarmBuzz-main\Evidence\prereq_network_complete.flag" -ErrorAction SilentlyContinue

# 4) Run build again
cd C:\BarmBuzz-main
.\Run_BuildMain.ps1